#
# Local development environment variables (copy to api/.env)
#
# 1) Create a file named: api/.env
# 2) Paste the variables below and fill in values
# 3) Do NOT commit api/.env
#

# MongoDB Atlas connection string (must include DB name)
# NOTE: URL-encode special characters in the password (e.g., @ -> %40, # -> %23)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/revocart?retryWrites=true&w=majority&appName=Cluster0&authSource=admin

# JWT secrets (dev values are OK locally; use strong secrets in production)
JWT_ACCESS_SECRET=dev_access_secret_change_me
JWT_REFRESH_SECRET=dev_refresh_secret_change_me

# Frontend origin for CORS
FRONTEND_URL=http://localhost:3000


